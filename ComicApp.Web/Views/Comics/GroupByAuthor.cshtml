@model IEnumerable<IGrouping<string, ComicApp.Core.Models.Comic>>

@{
    ViewData["Title"] = "Grouped by Author";
}

<h1>Comics Grouped by Author</h1>

<a asp-action="Index">Back to Comics</a>

@foreach (var group in Model)
{
    <h3>@group.Key</h3>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Title</th>
                <th>Year</th>
                <th>Genres</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var comic in group)
            {
                <tr>
                    <td>@comic.Title</td>
                    <td>@comic.PublicationYear</td>
                    <td>@string.Join(", ", comic.Genres)</td>
                </tr>
            }
        </tbody>
    </table>
}
